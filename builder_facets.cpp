/* an implementation of builder that is far more complicated 
 * we are going to be using two builders 
 * we need to use seperaet builder for sepeare concern
 * we wil build builder faceut /
 *
 *
 */

